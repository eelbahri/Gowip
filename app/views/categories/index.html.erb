<p id="notice"><%= notice %></p>



<div class="col-lg-12">
    <h1>Choose a category</h1> <%= link_to 'Add a category', new_category_path, class: 'btn btn-primary' %>
    <br><br>
    <% if @categories.any? %>
        <% @categories.each do |c| %>
            <%= link_to c.name.capitalize, c, class: 'cube' %>
        <% end %>
    <% else %>
        Aucune catgorie pour le moment, créez en une !
    <% end %>
</div>



<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= category.description %></td>
        <td><%= link_to 'Show', category %></td>
        <td><%= link_to 'Edit', edit_category_path(category) %></td>
        <td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>
